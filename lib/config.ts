/**
 * ============================================================================
 * SITE CONFIGURATION
 * ============================================================================
 *
 * Configurações de conteúdo/comportamento usadas pelos componentes.
 *
 * Marca, URL, autores e metadados de SEO/GEO vivem em `lib/metadata.ts`
 * (fonte única, consumida por layout, sitemap, robots e schema.org).
 */

export const pricingConfig = {
  title: "Diferencial Técnico Real",
  description:
    "Comparativo técnico entre o padrão do mercado e a autonomia do Kmillion.",
  billingNote: "",
};

/**
 * ============================================================================
 * FEATURE FLAGS
 * ============================================================================
 *
 * Toggle features on/off without touching component code.
 */

export const features = {
  smoothScroll: true,
  testimonialAutoplay: true,
  parallaxHero: true,
  blurInHeadline: true,
};
